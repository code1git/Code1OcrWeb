<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code1system.web.dbmapper1.Db1Mapper">
	<select id="getUserList"  resultType="com.code1system.web.dto.UserDto" parameterType="com.code1system.web.dto.UserDto">
		SELECT *
		FROM
		CODE1OCR_USER
		<trim prefix = "WHERE" prefixOverrides="AND" suffixOverrides =",">
			<if test="user_name!=null and user_name!=''">
				AND user_name = #{user_name}
			</if>
			<if test="user_id!=null and user_id!=''">
				AND user_id = #{user_id}
			</if>
			<if test="organization!=null and organization!=''">
				AND organization = #{organization}
			</if>
		</trim>
		ORDER BY user_id ASC
		LIMIT #{length} OFFSET #{start}
	</select>
	
	<select id="getUserById" resultType="com.code1system.web.dto.UserDto" parameterType="com.code1system.web.dto.UserDto">
		SELECT *
		FROM
		CODE1OCR_USER
		WHERE
		user_id = #{user_id}
	</select>
</mapper>